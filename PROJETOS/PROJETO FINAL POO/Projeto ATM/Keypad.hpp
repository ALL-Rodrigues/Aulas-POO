// Keypad.hpp
// definição da classe keypad. representa o teclado do ATM.

#ifndef KEYPAD_HPP
#define KEYPAD_HPP

class Keypad{
    public:
        int getInput() const; // retorna um valor de inteiro onserido pelo usuário 
    private:
        // não houve funções e atributos privados nessa classe
}; // fim da classe keypad

#endif // KEYPAD_HPP